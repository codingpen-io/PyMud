{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "v6_파일읽기",
      "type": "python",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "cwd": "${fileDirname}"
    }
  ]
}